# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.ipynb_checkpoints/

# macOS
.DS_Store

# Tooling
.ruff_cache/
.pytest_cache/
wandb/

# MorphoMNIST data and generated outputs (large / reproducible)
*.npz
# ...but the small cached causl test fixtures ARE tracked (needed by the
# gated ATE-recovery tests; regenerated via tests/fixtures/_generate.py).
!tests/fixtures/*.npz

# Claude context files (personal working files) — never tracked
.claude/
